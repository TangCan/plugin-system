---
epic: 2
date: 2026-08-18
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 Retrospective — 可安装且可守门

## Epic summary

- **史诗：** Epic 2 可安装且可守门（FR6–FR9）
- **Diff 范围：** `81512f3..3c403ac`
- **提交：** `3c403ac` Point public crates at GitHub and gate CI with clippy.（2.1 与 2.2 同一提交）
- **故事：** `2-1-公开-crate-指向-github-并具备上架清单`、`2-2-github-actions-与-clippy-门禁` 均为 `done`；`pending_stories: []`（`detect-epic --epic 2`）
- **sprint-status：** `epic-2: done`，本 retro 前 `epic-2-retrospective: optional`
- **证据库存**
  - 有：`epics.md` Epic 2、PRD §4.2–4.3、architecture AD-5/AD-6、两则短故事文件、`2-code-review.md`、`git_evidence.py`、`.github/workflows/ci.yml`、`scripts/ci-test.sh`
  - 缺：提交 subject 无 `2-1`/`2-2`，`commits[].stories` 空
  - 缺：未调用 `bmad-review` 子技能；透镜 inline
  - 缺：会话日志未摘录
  - 上一则 retro：`epic-1-retro-2026-08-18.md`（无 action items）

## Findings

### Architecture delta

- **观察：** 新增托管 CI：根 `.github/workflows/ci.yml` 在 `push`/`pull_request` 调 `./scripts/ci-test.sh`。`ci-test.sh` 增加 `cargo clippy -p plugctx -p plugctx-derive -- -D warnings`。工作区 `repository = "https://github.com/TangCan/plugin-system"`。无新运行时依赖进入 `plugctx` 默认图。
  - **来源：** `.github/workflows/ci.yml` L1–L16；`scripts/ci-test.sh` L12–L13；`Cargo.toml` L27；`3c403ac` `files`
  - **处置：** accept as-is
  - **课：** 无

### Duplication map

- **观察：** 2.1 与 2.2 合成一次提交、两份极短故事文件。功能不重复，但故事边界在 git 上不可分。
  - **来源：** `git_evidence.py` `commit_count: 1`；`2-1-公开-crate-指向-github-并具备上架清单.md`；`2-2-github-actions-与-clippy-门禁.md`
  - **处置：** accept as-is
  - **课：** 提交 subject 写故事 id；一提交跨两则故事时 `git_evidence` 无法自动归属

### God-class / size growth

- **观察：** 最大 churn 为文档与元数据（`docs/publishing.md` +11/−1），无内核类膨胀。
  - **来源：** `git_evidence.py` `files`
  - **处置：** accept as-is

### Pattern divergence

- **观察：** CI 未使用 `cargo test --all-features`（`scripts/ci-test.sh` 无该字符串）。符合 FR9 / NFR9，刻意偏离需求正文 §8.9 的「全量 --all-features」句——feature-matrix 已说明 `thread-safe` 互斥。
  - **来源：** `rg --all-features scripts/ci-test.sh` 无匹配；`docs/feature-matrix.md` 依赖边段；`2-code-review.md`
  - **处置：** accept as-is（已记录的偏离）
  - **课：** 无

### Spec-to-implementation

- **观察：** FR6 `repository`、FR8 GHA、FR9 clippy 均落地。FR7 故事完成标准允许无 token 时只做 dry-run + 清单；维护者随后实际上架 `plugctx`/`plugctx-derive` 0.1.0，并由 `0e59499` 改 publishing/CHANGELOG（范围外，视为超额交付）。
  - **来源：** `acceptance_story_9_1.rs`（本 retro 3 passed，含 dry-run）；`acceptance_story_9_3.rs`（2 passed）；`docs/publishing.md` 上架记录（`0e59499`）
  - **处置：** accept as-is
- **观察：** `workspace.package.authors = ["release_check"]` 仍在，且已打进 crates.io 0.1.0。本史诗改了同一 `Cargo.toml`（加 `repository`），未改 authors。AC 未要求 authors，但该占位符已随首次 publish 出去。
  - **来源：** `Cargo.toml` L25；crates.io 不可改已发布 0.1.0
  - **处置：** defer → action item（下一版本改 authors 再发）
  - **课：** 上架前扫一遍 package 元数据占位符，不要只盯 repository

### Diff-scope lenses（inline）

- **Adversarial：** workflow 无 registry token；clippy `-D warnings` 仅默认同步可发布 crate。与「空 default」一致。
  - **来源：** `ci.yml`；`ci-test.sh` L12–L13
  - **处置：** accept as-is
- **Edge-case：** 仅 `ubuntu-latest`（PRD 非目标：Windows 矩阵）。
  - **来源：** `ci.yml` L9；PRD §6.2
  - **处置：** accept as-is
- **Verification-gap：** 本 retro 未重跑完整 `./scripts/ci-test.sh` / 未打开 GitHub Actions run 页；只跑了 9.1/9.3 验收（含 publish dry-run）。托管 CI 绿以 `main` 上已有 workflow 为准，本文件不声称核验了某一 run id。
  - **来源：** 本 retro 测试输出；范围收窄如上
  - **处置：** accept as-is（故事 AC 由 9.3 护栏覆盖脚本存在性）

## Behavior verification

已跑：

```
cargo test -p plugctx --test acceptance_story_9_1 --test acceptance_story_9_3
```

5 passed（含 `cargo_publish_dry_run_succeeds_for_public_crates`、`ci_publish_dry_run_gate_blocks_on_failure`）。

未在本 retro 重跑全量 `ci-test.sh`（范围收窄：发布/CI 文档护栏，而非完整矩阵）。

## Previous-retro follow-through

Epic 1 retro（`epic-1-retro-2026-08-18.md`）**没有**登记 action items。sprint-status 在本史诗 retro 写入前仍无待办可 follow-through。不是「未检查」。

## Action items

| 拟议 | 所有者 | 说明 |
| --- | --- | --- |
| 下一版本将 `workspace.package.authors` 从 `release_check` 改为真实维护者后再发 crates.io（不可改 0.1.0） | Richard | 提议，不在本 retro 改代码 |

## Acceptance verdict

**accepted-with-open-items**（criteria: **declared** — epics FR6–FR9 / 故事 2.1–2.2）。

FR 已满足；唯一跟踪项是 authors 占位符随 0.1.0 上架。`pending_stories` 空。机器裁决，无人工改判。

## Open questions

无。trusted publishing / release-plz 全自动发版仍是 PRD 非目标。

## Assumptions

- Headless 批处理 `--epic 2`。
- `detect-epic --epic 2`：`story_count: 2`，`pending_stories: []`。
- Phase 3 跳过；`bmad-review` inline。
- 机器裁决 **accepted-with-open-items**。
- authors 修复登记为提议 action，不自动改 `Cargo.toml`。
- 未跑 `--set-action-status`（无上一条目）。
