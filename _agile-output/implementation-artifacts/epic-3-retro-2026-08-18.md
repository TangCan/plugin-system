---
epic: 3
date: 2026-08-18
verdict: accepted
criteria: declared
headless: true
---

# Epic 3 Retrospective — 可跟做的接入路径

## Epic summary

- **史诗：** Epic 3 可跟做的接入路径（FR10–FR13）
- **Diff 范围：** `3c403ac..a272a26`
- **提交：** `a272a26` Add CLI, Web, and game examples plus a user guide.（3.1–3.4 同一提交）
- **故事：** sprint-status 中 `3-1`…`3-4` 均为 `done`；`pending_stories: []`（`detect-epic --epic 3`）
- **故事文件：** 无独立 `3-1`/`3-2`/`3-3`/`3-4` 文件，仅 `_agile-output/implementation-artifacts/3-scene-examples.md` 合并记录
- **sprint-status：** `epic-3: done`，本 retro 前 `epic-3-retrospective: optional`
- **证据库存**
  - 有：`epics.md` Epic 3、PRD §4.4–4.5、architecture AD-8、合并故事/`3-scene-examples.md`、`docs/guide.md`、`examples/*`、`acceptance_story_5_3.rs` 护栏、本 retro 三次 `cargo run`
  - 缺：四则故事无分文件；`commits[].stories` 空
  - 缺：未调用 `bmad-review` 子技能
  - 缺：会话日志未摘录
  - 上一则 retro：`epic-2-retro-2026-08-18.md`（一条 authors action，本 retro 时仍为提议）

## Findings

### Architecture delta

- **观察：** 新增工作区成员 `examples/`（`plugctx-examples`，`publish = false`）。`tiny_http` 仅 `web` feature 可选依赖。`plugctx` 的 `Cargo.toml` 不含 `tiny_http`。CLI 示例 `required-features = ["native"]`。
  - **来源：** `examples/Cargo.toml` L1–L47；`crates/plugctx/tests/acceptance_story_5_3.rs` `tiny_http must not enter plugctx`（本 retro 该测试 ok）
  - **处置：** accept as-is（NFR1 / NFR5 / AD-8）
  - **课：** 无

### Duplication map

- **观察：** 三条示例各自一小文件（CLI 52 / Web 69 / game 55 行），无互相复制的加载器。指南命令与示例文件头注释重复——有意的可抄作业，不是第二套实现。
  - **来源：** `wc -l examples/*.rs`；`docs/guide.md` L51–L67；各 example 文件头
  - **处置：** accept as-is

### God-class / size growth

- **观察：** 本范围无 `context.rs` / `dynamic_native.rs` 增长。新文件都小。
  - **来源：** `git_evidence.py` `files`（最大实现 `examples/web-service.rs` net +69）
  - **处置：** accept as-is

### Pattern divergence

- **观察：** 四则故事合成一份 `3-scene-examples.md` + 一次 git 提交，与 Epic 1「一故事一文件一提交」不一致。
  - **来源：** `3-scene-examples.md`；`git_evidence.py` `commit_count: 1`
  - **处置：** accept as-is（交付完整；跟踪粒度变粗）
  - **课：** 场景切片若仍要分故事，至少保留分文件，便于 retro / `git_evidence` 归属

### Spec-to-implementation

- **观察：** FR10 指南存在且 README 链接；FR11 CLI 走 `load_native_plugin` + `handle.dispose` + 旧 invoker 失败；FR12 Web 用 `tiny_http` 0.12；FR13 无引擎 tick，卸载后 hits 不再增加。
  - **来源：** `docs/guide.md`；`examples/cli-hotplug.rs` L37–L51；`examples/web-service.rs` L15 / Cargo.toml `tiny_http = { version = "0.12", optional = true }`；`examples/game-loop.rs`
  - **处置：** accept as-is

### Diff-scope lenses（inline）

- **Adversarial：** CLI 找不到 `.so` 时 `panic!`（示例，非库 API）。库路径仍返回 `Result`。可接受。
  - **来源：** `examples/cli-hotplug.rs` L34
  - **处置：** accept as-is
- **Edge-case：** Web 默认自检一次后退出，避免 CI/演示挂起；常驻靠 `PLUGCTX_WEB_SELFTEST=0`。默认 `Context` 非 `Send`，自检把 client 放进线程（`3-scene-examples.md` code review 注）。
  - **来源：** `examples/web-service.rs` L8–L9、L39+；`3-scene-examples.md`
  - **处置：** accept as-is
- **Verification-gap：** 护栏测试检查指南字符串与 `tiny_http` 不进 plugctx，不 `cargo run` 三条示例。本 retro 补跑了三次 example（见下）。
  - **来源：** `acceptance_story_5_3.rs` L215–L234 vs 本 retro 运行记录
  - **处置：** accept as-is（缺口由本 retro 行为检查补上，不另开 action）

## Behavior verification

本机已执行（均 exit 0）：

1. `cargo build -p hello_plugin` 后 `cargo run -p plugctx-examples --example cli-hotplug --features native`  
   输出含 `hello, cli` 与 `after dispose: 插件 \`hello\` 调用 \`greet\` 失败（status=1）`
2. `cargo run -p plugctx-examples --example web-service --features web`  
   自检 HTTP 200，body `hello from plugctx`
3. `cargo run -p plugctx-examples --example game-loop`  
   `tick 3 hits=3` 后 `after unload hits=3 (must stay 3)`

另：`cargo test -p plugctx --test acceptance_story_5_3` 中 `guide_exists_and_readme_links_it` ok。

## Previous-retro follow-through

- **id：** `epic-2-retro-item-1-下一版本将-workspace-package-authors-从-releas`（sprint-status 原文）
- **是否落地：** 无证据。`Cargo.toml` L25 仍为 `authors = ["release_check"]`
- **拟议状态：** 无（仍应 `open`）。Headless 不传 `--set-action-status`

Epic 1 无 action items。

## Action items

无本史诗 fix-now。authors 项留在 Epic 2 名下。

## Acceptance verdict

**accepted**（criteria: **declared** — FR10–FR13 / 故事 3.1–3.4）。

指南、三则 `cargo run`、`publish = false`、tiny_http 未进 plugctx 均有来源。`pending_stories` 空。机器裁决，无人工改判。

## Open questions

无。

## Assumptions

- Headless 批处理 `--epic 3`。
- `detect-epic --epic 3`：`story_count: 4`，`pending_stories: []`。
- 合并故事文件被接受为该史诗的故事规格。
- Phase 3 跳过；`bmad-review` inline。
- 机器裁决 **accepted**。
- 不把 Epic 2 authors 项标 done。
